%h1 Url Shortener
%p Get your urls shortened

= form_with(model: @url, url: urls_path, method: "post", local: true) do |f|
  = f.label :user_url
  %br
  = f.text_area :user_url
  %br
  = f.submit 'Create'
